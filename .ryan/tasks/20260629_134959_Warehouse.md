# Ryan Task 20260629_134959_Warehouse

- Started: 2026-06-29 13:49:59 
- Project: Warehouse
- Root: /Users/oldking/Documents/Codex/RyanProjects/Warehouse
- Route: 命中关键词：OPC、YQN
- Confidence: matched

## User Request

# lark-channel-bridge 运行约定

你正在 lark-channel-bridge 里跑：把飞书/Lark 用户消息桥到本地 agent CLI。

## bridge_context

每条 user message 顶部会带一个 `<bridge_context>` 块：

```
<bridge_context>
{"chatId":"oc_xxx","chatType":"p2p","senderId":"ou_xxx","senderName":"...",
 "senderType":"user|bot","botOpenId":"ou_xxx","mentions":[{"openId":"ou_xxx","name":"...","isBot":true}], ...}
</bridge_context>
```

里面是当前对话的 chat_id、chat 类型（p2p / group）、发送者。关键字段：

- `senderType`：发送者是人（`user`）还是另一个 bot（`bot`）；缺省表示未知
- `botOpenId`：**你自己**的 open_id
- `mentions`：这条消息 @ 到的账号列表（含 open_id 和 isBot），需要 @ 某人/某 bot 时从这里取 id

多条消息在短时间内合并送达时，`user_input` 里每段会带 `[名字 (user|bot)]:` 行首标注以区分发送者——这是 bridge 注入的展示格式，**你回复时不要模仿这种标注**。这些都是 bridge 注入的元数据，**不要照抄、不要在你的回复里渲染**——它对用户不可见。

## 与其他 bot 协作（bot-at-bot）

- 自我识别：`bridge_context.botOpenId` 是你自己的 open_id；消息内容或 mentions 里出现这个 id 就是指你自己。
- 飞书机制：bot **只有被真实 @（结构化 mention）才能收到群消息**。纯文本写 "@名字"、或不带 @ 的普通回复，其他 bot 一律收不到。这条限制只针对 bot——人类用户能看到群里所有消息，回复人类不需要 @。
- 需要某个 bot 接着处理时，必须真实 @ 它（open_id 优先从 `bridge_context.mentions` 里取）。除此之外**默认不要 @ 其他 bot**——互相 @ 会形成死循环；用户明确要求转交/通知某个 bot 时按要求执行。
- 与其他 bot 对话时，没有新信息要补充就简短收尾，不要追问、不要客套往返。

## quoted_message

如果用户用"引用回复"指向某条消息，bridge 会在 `<bridge_context>` 后注入一个 `<quoted_message>` 块：

```
<quoted_message id="om_xxx" sender_id="ou_xxx" sender_name="..." created_at="..." type="text|merge_forward|...">
（被引用消息的内容；merge_forward 类型会展开成 <forwarded_messages>...</forwarded_messages>）
</quoted_message>
```

这是用户**指向的对象**——用户的实际问题在它之后。回答时围绕这段内容展开；它也是 bridge 注入的元数据，**不要照抄 XML 标签**到回复里。

## interactive_card

用户发 / 引用交互卡片时,bridge 会把卡的真实 JSON 注入到 `<interactive_card>` 块:

```
<interactive_card>
{ "schema": "2.0", "config": { ... }, "body": { ... } }
</interactive_card>
```

两种来源:

- **v2 CardKit (schema 2.0)**:飞书在 raw event 里双发——`elements` 是 v1 兼容降级("请升级至最新版本客户端"),`user_dsl` 是真正的 schema 2.0 DSL。bridge 优先取 `user_dsl`,所以你看到的就是**真卡内容**,不要被 elements 的降级文案误导
- **零文字 v1 卡**:纯按钮 / 图片 / 装饰卡,SDK 扁平化抓不到字时,bridge 把整段 raw JSON 灌进来

无论哪种,块里都是卡的完整 JSON。解析它来理解结构(按钮、字段、布局)。**不要照抄 XML 标签到回复**——对用户不可见。

## 发交互卡片（按钮、表单）的回调约定

你想发一张可交互的卡片让用户点选时：

1. 用 `lark-cli` 把卡发到 `bridge_context.chat_id`：
   `lark-cli im send-card --chat-id <chat_id> --card '<json>'`
2. 卡片用 CardKit 2.0 schema（`schema: "2.0"`）。
3. **如果你希望用户点按钮后回调到你（让你在同一会话里继续处理）**：
   - 按钮的 `value` 对象**必须**同时包含 `__bridge_cb: true` 和 `bridge_token: "<signed token>"`。
   - `bridge_token` 必须由 bridge-aware 的 lark-cli 回调签名能力生成；不要猜测、伪造、复用或手写 token。
   - 如果当前 lark-cli 不能生成 `bridge_token`，不要发送回调按钮。改成普通展示卡，让用户用文字回复选择。
   - 同时可以塞任意其它字段，作为你需要在回调时记住的状态（比如 `choice`、`ticket_id`）。
4. 用户点击后，bridge 会校验 `bridge_token`，然后把 payload（去掉 `__bridge_cb` 和 `bridge_token`）作为 `[card-click] {...}` 消息发回给你；你的 session 自动续上，能看到自己上轮发了什么卡。
5. **如果只是展示卡（不需要回调）**，不要加 `__bridge_cb` 或 `bridge_token`，否则点击会被当成回调并要求签名。

示例 button：
```json
{
  "tag": "button",
  "text": { "tag": "plain_text", "content": "方案 A" },
  "behaviors": [{
    "type": "callback",
    "value": {
      "__bridge_cb": true,
      "bridge_token": "SIGNED_TOKEN_FROM_LARK_CLI",
      "choice": "a"
    }
  }]
}
```

## lark-cli 运行环境

bridge 会给你的子进程注入当前运行 profile 的环境变量:

- `LARK_CHANNEL=1`
- `LARK_CHANNEL_HOME`: 当前 bridge 的配置根目录
- `LARK_CHANNEL_PROFILE`: 当前 bridge profile
- `LARK_CHANNEL_CONFIG`: 当前 profile 的 lark-cli source projection
- `LARKSUITE_CLI_CONFIG_DIR`: 当前 profile 的 lark-cli 私有配置目录

因此普通 `lark-cli ...` 命令会自动进入当前 lark-channel 工作区,读取当前 profile 的私有 lark-cli 配置。不要 unset `LARK_CHANNEL` / `LARK_CHANNEL_HOME` / `LARK_CHANNEL_PROFILE` / `LARKSUITE_CLI_CONFIG_DIR`,也不要用 `env -u LARK_CHANNEL` 绕回本机普通配置。

如果 `lark-cli` 提示 `lark-channel context detected but lark-cli is not bound to it`,不要改用普通 profile,不要直接读取 `config.json` 里的账号或密钥,也不要自行执行 bind。停止当前操作并请用户重启 bridge 或运行 bridge doctor/preflight。

配置文件可能是多 profile 结构,不要假设根层一定有旧版单 profile 的 `accounts.app`;确实需要读取配置时按当前 profile 取值,且不要输出密钥。

## 飞书 OAuth 授权（`lark-cli auth login`）

授权流程要让 `lark-cli` 进程一直活到用户在浏览器里点完为止。bridge 在你的 run 结束之后会回收 agent 子进程，**你 spawn 的任何后台 bash 也会跟着死**——所以授权必须用"前台阻塞"的方式跑：

1. **仅在 p2p 里发起授权**。从 `bridge_context.chat_type` 看：
   - `chat_type: p2p` —— 正常按下面流程走。
   - `chat_type: group`（含 topic 群）—— **不要**调 `lark-cli auth login`。device flow 把 `verification_url` 发到群里，谁先点谁拿走 token——会绑定到错的身份。正确做法是回复用户："授权要在私聊里做，请单独私信我。"
2. **禁止** 用 `run_in_background: true` 调 `lark-cli auth login`——它会被你 exit 时一起带走，用户还没点完就丢了。
3. **推荐两阶段流**（lark-cli 在 `--no-wait` 的输出里也会告诉你这套）：
   - 先跑 `lark-cli auth login --no-wait --json [--recommend | --domain ... | --scope ...]`，**这一步秒返回**，stdout 里有 `verification_url` 和 `device_code`。
   - 把 `verification_url` **原样**用代码块发给用户（不要 Markdown 链接化、不要 URL 编码）。
   - 紧接着同一轮里跑 `lark-cli auth login --device-code <code>`，**这一步前台阻塞**直到用户点完或 10 分钟超时——这是你应该等的地方，不要丢到后台。
4. `lark-cli auth login --device-code <code>` 成功后,继续在同一个当前 profile 环境里执行:
   - `lark-cli config strict-mode off`
   - `lark-cli config default-as auto`
   这会让当前 profile 同时可用应用身份和已授权用户身份。不要重新 bind,不要绕回本机普通配置。
   这是内部顺序执行身份策略收敛,不要把 strict-mode/default-as 这类内部配置命令展示给用户,也不要让用户判断这些命令。面向用户只说："当前 profile 还没有可用的用户身份授权,请打开下面链接完成授权;授权完成后我会继续处理。"
5. 如果当前 profile 已经有用户授权,但 `--as user` 仍被 strict-mode/default-as 拒绝,不要向用户展示内部命令;在用户明确要求使用用户身份时,内部顺序执行身份策略收敛后重试原命令。
6. 你前台阻塞期间，用户发的新消息 bridge 会自动排队，**不会打断你**；等你 tool_result 一回来，下一批消息再进来。所以放心阻塞。
7. 如果用户中途想取消，他们会发 `/stop`——那时被 kill 是预期行为，不用兜底。

## 你的身份

你的 open_id 是 `ou_bb621fada5b474ca655ac85f256eadb3`，名字是「Ryan」。消息内容或 mentions 里出现这个 open_id 都是指你自己。


## user_message

<bridge_context>
{"chatId":"oc_d89c45bb9a041dee17375620cf26fb1f","chatType":"p2p","senderId":"ou_1189fbd2ab38bc3f0ba487cca795b747","senderType":"user","botOpenId":"ou_bb621fada5b474ca655ac85f256eadb3","messageIds":["om_x100b6b14369418a0c032b801a81821f"],"source":"im"}
</bridge_context>

<bridge_instructions>
["你在 bridge 进程中运行，普通 lark-cli 会继承 LARK_CHANNEL=1 并进入 bridge-bound 模式。","不要 unset LARK_CHANNEL / LARK_CHANNEL_HOME / LARK_CHANNEL_PROFILE / LARKSUITE_CLI_CONFIG_DIR，也不要用 env -u LARK_CHANNEL 绕回本机普通配置。","Codex bridge 默认使用 danger-full-access 对齐 Claude bridge 的 bypassPermissions 行为，因此 lark-cli 应能像用户本机终端一样访问 keychain。","如果提示 lark-channel context detected but not bound，停止当前操作并请用户重启 bridge 或运行 bridge doctor/preflight；不要改用普通 profile，不要自行 bind，也不要直接读取 config.json 里的账号或密钥。"]
</bridge_instructions>

<user_input>
{"text":"请先解决“独立项目桌面 / 不污染当前工作桌面”这个 P0 问题。不要再做简单 open URL / open Finder 的版本。只要无法保证窗口都在独立 Space，就必须安全失败，不准污染我当前正在工作的桌面。\n你说得对，这版没满足你的核心需求。问题不是你操作错了，是我这个 V0/V0.1 的方案还停留在“打开一堆东西”，没有真正做到**隔离工作区**。\n下面这段你可以直接复制给 Codex。\n**给 Codex 的需求文档 / Prompt**\n你现在要开发一个 **macOS 项目工位启动器 V0.2**。\n目标不是简单打开网页、文件夹、md，而是解决一个核心问题：\n用户正在某个 Mac 桌面 / Space 上工作时，点击某个项目启动器，不能污染当前工作桌面，不能往当前 Chrome 窗口里塞标签，不能把 Finder / md / TextEdit 窗口开到当前工作桌面。必须新建或切换到一个独立的项目桌面，在那个桌面里打开所有项目窗口。\n\n**一、背景**\n用户是纯小白，不懂路径、Cursor、CLI、项目目录。\n用户只想点击一个入口，例如：\n\n```PLAIN_TEXT\nOPC开工.command\n\n```\n\n然后系统自动进入一个独立的项目工位。\n项目示例：\n\n```PLAIN_TEXT\nOPC 小红书封面工具工位\n\n```\n\n里面需要打开：\n\n```PLAIN_TEXT\n1. ChatGPT 项目对话\n2. Canva\n3. OPC 项目文件夹\n4. 今日开工记录\n5. 输出文件夹\n6. 参考案例文件夹\n7. 未来 Codex / 网页工具入口\n\n```\n\n**二、当前失败点**\n上一版的问题：\n\n```PLAIN_TEXT\n1. 点击 command 后，Chrome 链接会打开到用户当前正在工作的 Chrome 窗口里，污染原来的网页环境。\n\n2. Finder 文件夹会打开到当前桌面，覆盖用户正在工作的桌面。\n\n3. md / TextEdit 窗口会打开到当前桌面，影响用户原本工作。\n\n4. “新建桌面打开”没有真的新建一个 macOS 桌面 / Space。\n\n5. 即使从桌面 3 点击，也不能保证所有窗口都在桌面 3 或新的项目桌面里。\n\n```\n\n这些都不合格。\n**三、核心需求**\n**需求 1：必须隔离当前工作桌面**\n用户点击启动器时，当前桌面上已有的东西不能被影响。\n不允许：\n\n```PLAIN_TEXT\n把项目网页加入当前 Chrome 窗口\n把 Finder 打开在当前工作桌面\n把 md / TextEdit 打开在当前工作桌面\n覆盖当前窗口布局\n\n```\n\n允许：\n\n```PLAIN_TEXT\n用户点击后，系统切到一个新的项目桌面 / Space\n所有 OPC 项目窗口只出现在这个项目桌面\n用户返回原桌面时，原来的网页和窗口保持原样\n\n```\n\n**需求 2：必须创建或复用独立项目桌面**\n启动逻辑应该是：\n\n```PLAIN_TEXT\n点击 OPC开工.command\n↓\n检查是否已有 OPC 项目桌面\n↓\n如果已有，切换到 OPC 项目桌面\n↓\n如果没有，新建一个 macOS Space / 桌面\n↓\n切换到这个新桌面\n↓\n再打开 OPC 的所有窗口\n\n```\n\n重点：\n\n```PLAIN_TEXT\n先切到项目桌面，再打开项目内容。\n\n```\n\n不能先打开内容再尝试移动。\n**需求 3：如果无法新建桌面，必须安全失败**\n不能假装成功。\n如果 macOS 原生自动化无法稳定新建 Space，需要：\n\n```PLAIN_TEXT\n1. 停止打开任何窗口\n2. 弹窗告诉用户：未能创建独立项目桌面，已停止，避免污染当前桌面\n3. 给出下一步方案，例如安装免费依赖 yabai / Hammerspoon / AeroSpace\n\n```\n\n绝对不能：\n\n```PLAIN_TEXT\n新建桌面失败后，直接在当前桌面打开项目窗口。\n\n```\n\n**四、Chrome 需求**\nChatGPT 和 Canva 必须打开在一个新的、独立的 Chrome 窗口中。\n不允许：\n\n```PLAIN_TEXT\nopen https://xxx\n\n```\n\n因为这会污染用户当前 Chrome 窗口。\n应该尽量做到：\n\n```PLAIN_TEXT\n1. 新开 Chrome 窗口\n2. 使用独立 profile 或独立 app/window\n3. 项目链接只进入 OPC 项目 Chrome 窗口\n4. 不影响用户原来 Chrome 里的标签页\n\n```\n\n目标效果：\n\n```PLAIN_TEXT\n原 Chrome 工作窗口：不变\n\n新 Chrome 项目窗口：\n- ChatGPT OPC 项目对话\n- Canva\n- 其他 OPC 链接\n\n```\n\n**五、Finder / 文件夹需求**\n项目文件夹必须在项目桌面打开。\n需要自动创建：\n\n```PLAIN_TEXT\n~/AI工位/OPC小红书封面工具/\n\n```\n\n里面包含：\n\n```PLAIN_TEXT\n00_入口\n01_商品资料_链接截图\n02_小红书参考案例\n03_封面素材\n04_Canva导出\n05_GPT输出文案\n06_最终成品\n07_Codex_未来开发\n08_归档\n\n```\n\n但用户不需要知道这个路径。\n启动时：\n\n```PLAIN_TEXT\n1. 先进入项目桌面\n2. 再打开 Finder 新窗口\n3. Finder 指向 OPC 项目文件夹\n\n```\n\n不能打开到用户当前工作桌面。\n**六、md / 文档需求**\n不要用系统默认方式乱开 md。\n上一版打开 md 后会跑到奇怪地方。\nV0.2 要求：\n优先方案：\n\n```PLAIN_TEXT\n不要直接打开 md 文件。\n改为生成一个本地 HTML 工位首页。\n然后在 OPC 项目的 Chrome 新窗口里打开这个 HTML。\n\n```\n\n例如：\n\n```PLAIN_TEXT\n00_入口/OPC工位首页.html\n\n```\n\n里面显示：\n\n```PLAIN_TEXT\n今日开工记录\n今日要做\n常用链接\n文件夹说明\n下一步动作\n\n```\n\n这样 md 不会再污染桌面。\n如果必须打开 md，也必须保证：\n\n```PLAIN_TEXT\n先切换到项目桌面\n再用指定应用新窗口打开\n不能用当前桌面的默认 md 软件打开\n\n```\n\n**七、启动器文件需求**\n需要生成这些入口：\n\n```PLAIN_TEXT\nOPC开工.command\n\n```\n\n这是唯一主入口。\n可以放在：\n\n```PLAIN_TEXT\n用户主桌面\n\n```\n\n但它只是入口，不代表项目窗口必须开在主桌面。\n点击后必须：\n\n```PLAIN_TEXT\n创建 / 切换 OPC 项目桌面\n然后在 OPC 项目桌面打开所有内容\n\n```\n\n不要再做一堆容易混淆的入口，比如：\n\n```PLAIN_TEXT\n当前桌面打开\n右边桌面打开\n新建桌面打开\n\n```\n\n用户不想选。\n用户只需要一个按钮：\n\n```PLAIN_TEXT\nOPC开工\n\n```\n\n系统自己处理隔离。\n**八、配置文件需求**\n需要有一个简单配置文件：\n\n```PLAIN_TEXT\n{\n  \"projectName\": \"OPC小红书封面工具\",\n  \"workspaceRoot\": \"~/AI工位/OPC小红书封面工具\",\n  \"chatgptUrl\": \"https://chatgpt.com/g/g-p-69b97436b40c8191af4cc856df4cf42c/c/6a3e45c0-a11c-83ec-83cd-cb994548ce9f\",\n  \"links\": [\n    {\n      \"name\": \"ChatGPT OPC 项目对话\",\n      \"url\": \"https://chatgpt.com/g/g-p-69b97436b40c8191af4cc856df4cf42c/c/6a3e45c0-a11c-83ec-83cd-cb994548ce9f\"\n    },\n    {\n      \"name\": \"Canva\",\n      \"url\": \"https://www.canva.com/\"\n    }\n  ],\n  \"folders\": [\n    \"00_入口\",\n    \"01_商品资料_链接截图\",\n    \"02_小红书参考案例\",\n    \"03_封面素材\",\n    \"04_Canva导出\",\n    \"05_GPT输出文案\",\n    \"06_最终成品\",\n    \"07_Codex_未来开发\",\n    \"08_归档\"\n  ]\n}\n\n```\n\n未来可以新增 YQN 工位、小红书工具工位、封面工具工位，不要把逻辑写死。\n**九、技术实现要求**\n优先级：\n**优先方案 A：原生 macOS 自动化**\n先尝试用：\n\n```PLAIN_TEXT\nAppleScript\nosascript\nSystem Events\nMission Control UI scripting\n\n```\n\n实现：\n\n```PLAIN_TEXT\n新建 Space\n切换到 Space\n再打开项目窗口\n\n```\n\n但是必须验证可靠性。\n**备用方案 B：免费依赖**\n如果原生方案无法稳定实现，允许使用免费工具，但必须说明原因。\n可考虑：\n\n```PLAIN_TEXT\nyabai\nHammerspoon\nAeroSpace\n\n```\n\n要求：\n\n```PLAIN_TEXT\n免费\n稳定\n兼容 Apple Silicon / M4 Pro\n尽量不要求关闭 SIP\n尽量不破坏系统\n\n```\n\n如果某个方案需要关闭 SIP，默认不推荐。\n**十、硬性验收标准**\n必须通过以下测试。\n**测试 1：不污染当前 Chrome**\n操作：\n\n```PLAIN_TEXT\n桌面 1 打开一个正在工作的 Chrome 窗口\n里面有 5 个标签页\n点击 OPC开工.command\n\n```\n\n合格结果：\n\n```PLAIN_TEXT\n桌面 1 的 Chrome 窗口不新增任何标签页\n不被覆盖\n不跳转\n不改变原有状态\n\n```\n\n**测试 2：项目窗口进入独立桌面**\n操作：\n\n```PLAIN_TEXT\n点击 OPC开工.command\n\n```\n\n合格结果：\n\n```PLAIN_TEXT\n系统切到新的 OPC 项目桌面\n这个桌面里出现：\n- OPC Chrome 新窗口\n- OPC Finder 新窗口\n- OPC 工位首页\n\n```\n\n不合格：\n\n```PLAIN_TEXT\n任何项目窗口出现在原工作桌面\n\n```\n\n**测试 3：新建桌面失败时保护现场**\n操作：\n\n```PLAIN_TEXT\n模拟新建 Space 失败\n点击 OPC开工.command\n\n```\n\n合格结果：\n\n```PLAIN_TEXT\n弹窗提示失败\n不打开任何项目网页\n不打开 Finder\n不打开文档\n\n```\n\n不合格：\n\n```PLAIN_TEXT\n失败后仍然污染当前桌面\n\n```\n\n**测试 4：重复点击不重复污染**\n操作：\n\n```PLAIN_TEXT\n连续点击 OPC开工.command 两次\n\n```\n\n合格结果：\n\n```PLAIN_TEXT\n第二次应该切回已有 OPC 项目桌面\n不要无限新建桌面\n不要无限打开重复窗口\n\n```\n\n**测试 5：小白可用**\n用户最终只需要：\n\n```PLAIN_TEXT\n双击 OPC开工.command\n\n```\n\n不需要知道：\n\n```PLAIN_TEXT\n路径\nCursor\nCLI\nGitHub\nHomebrew\nSpaces 原理\nAppleScript\n\n```\n\n**十一、最终交付物**\n请输出：\n\n```PLAIN_TEXT\n1. OPC开工.command\n2. install.command\n3. uninstall.command\n4. config.json\n5. README_小白使用说明.md\n6. 技术说明.md\n\n```\n\nREADME 必须写人话：\n\n```PLAIN_TEXT\n怎么安装\n怎么打开\n失败怎么办\n怎么加链接\n怎么换 ChatGPT 链接\n怎么卸载\n\n```\n\n**十二、产品原则**\n这个项目的核心不是“打开链接”。\n核心是：\n不污染当前工作现场，创建/恢复一个独立项目现场。\n如果做不到这一点，不要继续做“打开链接”的假功能。\n你可以把上面这整段直接扔给 Codex。\n我再给你一句更短的版本，方便你开头补一句："}
</user_input>

## Result

Ryan will append detailed results through `.ryan/worklog.md` and Git commits.
