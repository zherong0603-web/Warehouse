# 阶段 1 自测报告

## 1. 开发前置检查

已先读取 `07_项目宪法/项目宪法.md`。

阶段 1 未偏离以下原则：

- 只围绕美国仓 / 北美跨境电商：通过。
- 不把墨西哥仓作为重点：通过。
- 不让用户手动打分：通过。
- 不做普通新闻日报：通过。
- 不做复杂前端：通过。
- 不接飞书：通过。
- 不接 CRM / 企微生产系统：通过。
- 不做全网爬虫：通过。
- 所有判断必须有证据片段：通过。
- 不确定内容标记“待确认”：通过。
- 红灯和灰灯区分：通过，红灯是低质量，灰灯是证据不足。

## 2. 自测环境

- Python：3.9.6
- 实现方式：Python 标准库
- 双击入口：`运行阶段1.command`
- 支持输入格式：`.txt`、`.md`、`.csv`
- 不支持格式：PDF、Word、Excel、PPT

## 3. 样例运行结果

执行命令：

```bash
python3 run_stage1.py --input 00_输入收件箱/样例_阶段1
```

运行结果：

- 读取输入文件/记录：8 条。
- 识别 MQL：5 条。
- 绿灯：2 条。
- 黄灯：1 条。
- 红灯：1 条。
- 灰灯：1 条。
- 事实提取表：8 条事实记录。
- 内容实验库：6 条内容实验。
- 最终运行目录：`08_验收包/阶段1运行记录/2026-06-24_163248/`。

已生成并更新 6 个输出文件：

- `01_事实提取/事实提取表.csv`
- `02_MQL质量复盘/MQL质量复盘.md`
- `03_组织缺口雷达/组织缺口雷达.md`
- `04_内容实验库/内容实验库.csv`
- `05_作战日报/个人作战日报.md`
- `05_作战日报/管理层摘要.md`

## 4. 空输入测试

执行命令：

```bash
python3 run_stage1.py --input /private/tmp/yqn_stage1_empty_input
```

结果：

- 程序没有崩溃。
- 输出提示：“未发现输入文件。请把 .txt / .md / .csv 放入输入目录。”
- 已写入 `logs/stage1_2026-06-24.log`。

默认收件箱测试：

```bash
python3 run_stage1.py
```

结果：

- 当前 `00_输入收件箱/` 只有样例资料，默认运行会跳过文件名或目录名中带“样例”的资料。
- 程序没有把样例混入真实输出。
- 程序提示未发现输入文件，正常退出。

## 5. 技术校验

- `run_stage1.py` 语法检查：通过。
- `运行阶段1.command` zsh 语法检查：通过。
- `运行阶段1.command` 可执行权限：通过。
- 带日期运行目录中包含 6 个输出文件：通过。

## 6. 阶段 1 验收标准自检

1. 用户不需要手动打分：通过。
2. 系统能自动生成 6 个输出文件：通过。
3. 每条 MQL 判断有证据片段：通过。
4. 红灯和灰灯区分清楚：通过。
5. 内容实验能反向服务小红书获客：通过。
6. 个人日报能指导用户当天工作：通过。
7. 管理层摘要能给老板看：通过。
8. 不围绕墨西哥仓展开：通过。
9. 不输出空泛公司宣传：通过。
10. Codex 已经自测并提交结果，不要求用户自己测试：通过。

## 7. 自测结论

阶段 1 离线闭环 MVP 已达到“稳定可运行”的标准。它可以读取本地 txt/md/csv，自动生成 6 个中文输出，且每次运行保存带日期时间的副本。
